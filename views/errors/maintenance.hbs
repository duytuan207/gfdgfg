<!doctype html>
<html lang="vi">

<head>
    <title>{{#if title}}{{ title }} - {{ settings.defaultTitle }}{{else}}{{ settings.defaultTitle }}{{/if}}</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <meta name="title"
        content="{{#if title}}{{ title }} - {{ settings.defaultTitle }}{{else}}{{ settings.defaultTitle }}{{/if}}" />
    <meta name="description" content="{{ settings.description }}" />
    <meta name="keywords" content="{{ settings.keywords }}" />
    <meta name="robots" content="index, follow">
    <link href="{{ settings.favicon }}" rel="shortcut icon" type="image/x-icon" />
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="../themes/admin/css/plugin.min.css">
    <link rel="stylesheet" href="../themes/admin/css/style.css">
    {{{ settings.headerScript }}}
</head>

<body class="loaded">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-md-12">
                <div class="min-vh-100 content-center">
                    <div class="maintenance-page text-center">
                        <img src="../themes/admin/images/maintenance.svg" alt="maintenance" class="svg" />
                        <h5 class="maintenance-page__title">Hệ Thống Đang Tạm Bảo Trì</h5>
                        <p class="fw-500">Hệ thống đang tạm bảo trì để khắc phục 1 số lỗi, chúng tôi sẽ quay lại sớm nhất có thể!</p>
                    </div>
                </div>
            </div>
        </div>
    </div>